Serialize a binary tree, convert it into a list and then re construct a binary tree from it again. 


THe approach: Do a pre-order traversal. Convert the pre-order traversal into a list. Build tree again. 
